import { test } from '@playwright/test'

// Deprecated: replaced by tests/visual/* using expect(page).toHaveScreenshot()
// Keeping this file to avoid breaking older docs/links.

test.skip('screens: all key routes (deprecated)', async () => {})
